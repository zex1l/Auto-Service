import { LoadingOutlined } from "@ant-design/icons";

const Loader = () => {
    return (
        <div>
            <LoadingOutlined style={{fontSize: 64}}/>
        </div>
    );
};

export default Loader;